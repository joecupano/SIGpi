#!/bin/bash

###
### SIGpi
###
### pkg_bettercap-deb
###
###

case "$1" in 
    remove )
        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##   Remove bettercap"
        echo -e "${SIGPI_BANNER_RESET}"

        cd $SIGPI_PACKAGES        
        if [[ "$SIGPI_HWARCH" == "x86_64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -r bettercap_2.32.0-1_amd64.deb
            sed -i /sdrpp/d $SIGPI_CONFIG
        fi

        if [[ "$SIGPI_HWARCH" == "aarch64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -r bettercap_2.32.0-1_arm64.deb
            sed -i /sdrpp/d $SIGPI_CONFIG
        fi

        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##   bettercap Removed"
        echo -e "${SIGPI_BANNER_RESET}"
        ;;
    purge )
        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##   Purge bettercap"
        echo -e "${SIGPI_BANNER_RESET}"

        cd $SIGPI_PACKAGES        
        if [[ "$SIGPI_HWARCH" == "x86_64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -P bettercap_2.32.0-1_amd64.deb
            sed -i /sdrpp/d $SIGPI_CONFIG
        fi

        if [[ "$SIGPI_HWARCH" == "aarch64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -P bettercap_2.32.0-1_arm64.deb
            sed -i /sdrpp/d $SIGPI_CONFIG
        fi
   
        sudo rm -rf $SIGPI_DESKTOP/sdrpp.desktop
        sudo rm -rf $DESKTOP_FILES/sdrpp.desktop

        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##   bettercap Purged"
        echo -e "${SIGPI_BANNER_RESET}"
        ;;
    install )
        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##"
        echo -e "${SIGPI_BANNER_COLOR} ##   Install bettercap-deb"
        echo -e "${SIGPI_BANNER_COLOR} ##"
        echo -e "${SIGPI_BANNER_RESET}"
        
       
        cd $SIGPI_PACKAGES        
        if [[ "$SIGPI_HWARCH" == "x86_64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -i bettercap_2.32.0-1_amd64.deb
        fi

        if [[ "$SIGPI_HWARCH" == "aarch64" ]]; then
	        cd $SIGPI_PACKAGES
	        sudo dpkg -i bettercap_2.32.0-1_arm64.deb
        fi
        echo "sdrpp" >> $SIGPI_CONFIG 
        
	    ## DESKTOP
	    # Add Icon
        # Add Desktop
        sudo cp $SIGPI_HOME/desktop/bettercap.desktop $DESKTOP_FILES
	    # Change Category
        # Add to Menu
        xdg-desktop-menu install --novendor --noupdate $DESKTOP_DIRECTORY/SigPishell.directory $DESKTOP_FILES/bettercap.desktop
        # Change Category
	    #sudo sed -i "s/Categories.*/Categories=$SIGPI_MENU_CATEGORY;/" $DESKTOP_FILES/sdrpp.desktop
        # Add to Menu
        #xdg-desktop-menu install --novendor --noupdate $DESKTOP_DIRECTORY/SigPi.directory $DESKTOP_FILES/sdrpp.desktop
        
        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##   bettercap Installed"
        echo -e "${SIGPI_BANNER_RESET}"
        ;;
    * )
        echo -e "${SIGPI_BANNER_COLOR}"
        echo -e "${SIGPI_BANNER_COLOR} ##  ERROR: Unkown action or package"
        echo -e "${SIGPI_BANNER_RESET}"
        ;;
esac
